import styled from "styled-components";

export const FlexContainer_SB = styled.div`
  display: flex;
  justify-content: space-between;
`;

export const Wrapper = styled.div`
  max-width: 1200px;
  width: 100%;
  padding: 0 10px;
  margin: 0 auto;
`;

export const SocialItem = styled.a`

  cursor: pointer;
  svg {
    height: 15px;
    width: auto;
  }
`;
